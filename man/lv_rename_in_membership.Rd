% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rename.R
\name{lv_rename_in_membership}
\alias{lv_rename_in_membership}
\title{Point the membership tab at a renamed dataset}
\usage{
lv_rename_in_membership(renames, cfg, backend, dry_run = TRUE)
}
\arguments{
\item{renames}{From \code{\link[=lv_planned_renames]{lv_planned_renames()}}; unsafe rows are ignored.}

\item{cfg}{An \code{lv_config}.}

\item{backend}{A sheet backend.}

\item{dry_run}{Report without writing.}
}
\value{
A tibble of what was (or would be) patched.
}
\description{
\code{datasetsInCompilation} names datasets by name, so after a rename the old
entry matches nothing: the dataset drops out of the considered set and its
timeseries vanish from the QC tab on the following run, which looks like the
compilation losing a dataset for no reason.
}
\details{
Patches the cell in place, addressed by matching the old name, so the tab's
other rows and formatting are untouched.
}
