% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{lv_ingest_validate}
\alias{lv_ingest_validate}
\title{Validate incoming names and metadata before they enter LiPDverse}
\usage{
lv_ingest_validate(dir, index, progress = TRUE)
}
\arguments{
\item{dir}{Directory of incoming \code{.lpd} files.}

\item{index}{An \code{lv_index} of the live database.}

\item{progress}{Show progress.}
}
\value{
An \code{lv_issues} tibble.
}
\description{
Severity is calibrated against the live database rather than an ideal, so the
gate rejects what LiPDverse genuinely does not contain and merely reports what
it contains but would rather not:
}
\details{
\itemize{
\item \code{Site.Author.YYYY} covers 82\% of the database, not all of it — the rest are
legitimate short codes like \code{AB08MEN01}. A non-conforming name is a warning.
\item Every one of the 7,177 datasets has an \code{archiveType} and coordinates, so
their absence is an error.
\item An implausible year is an error even though 107 existing datasets fail it.
They exist because there was no gate; using them to justify admitting more
would be circular.
\item No dataset name contains a filesystem-hostile character, so that is an
error. Spaces and non-ASCII do occur (7 and 15 datasets), so they warn.
}

\code{writeLipd()} names its output from \code{dataSetName}, not from the incoming
filename, and every dataset in the database satisfies \code{dataSetName == file}.
So a disagreement is not an error, but it does mean the file lands under a
different name than it arrived with, which is worth saying out loud.
}
