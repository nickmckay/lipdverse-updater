% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab_review.R
\name{lv_vocab_review}
\alias{lv_vocab_review}
\title{Build a review file for unrecognised vocabulary}
\usage{
lv_vocab_review(
  issues,
  out,
  vocab = lv_vocab_overlay(store = store),
  store = qc_store(),
  n_candidates = 5,
  overwrite = FALSE
)
}
\arguments{
\item{issues}{The \code{issues} element of \code{\link[=lv_ingest_standardize]{lv_ingest_standardize()}}, or any
\code{lv_issues} tibble carrying \code{check == "unknown_vocabulary"}.}

\item{out}{Path to write.}

\item{vocab}{From \code{\link[=lv_vocab]{lv_vocab()}}; pass the overlaid vocabulary to avoid
re-offering values already decided.}

\item{store}{From \code{\link[=qc_store]{qc_store()}}.}

\item{n_candidates}{How many suggestions per row.}

\item{overwrite}{Replace an existing review file.}
}
\value{
The review tibble, invisibly.
}
\description{
One row per distinct \verb{(field, value)} still unrecognised after existing
decisions are applied, with the occurrence count, an example dataset, and the
closest candidates from the vocabulary. Fill in \code{decision} (and \code{map_to},
\code{also_field}, \code{also_value} where it applies) and feed it back to
\code{\link[=lv_vocab_apply_review]{lv_vocab_apply_review()}}.
}
\details{
Follows the review-file convention: generated once, then owned by the person
editing it. Regenerating over your edits would discard them, so this refuses
to overwrite an existing file unless \code{overwrite = TRUE}.
}
