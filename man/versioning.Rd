% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/version.R
\name{versioning}
\alias{versioning}
\title{Compilation versioning}
\description{
A compilation version is \code{publication_dataset_metadata}, e.g. \verb{1_0_3}:
}
\details{
\describe{
\item{\code{publication}}{Bumped by hand, when the compilation is published.}
\item{\code{dataset}}{Bumped when the set of datasets changed. Resets metadata.}
\item{\code{metadata}}{Bumped when the datasets are the same but their content
changed.}
}

Replaces \code{tickVersion()} (\code{lipdverseR/R/nightlyUpdateDrake.R:110}), which read
a Google Sheet inline and so could not be tested, and which decided the bump
with \code{all(lastUdsn == thisUdsn)} on two sorted character vectors. \code{==}
recycles when the lengths differ, so adding datasets could compare a name
against the wrong name and, when the recycled comparison happened to hold,
tick metadata for a run that had actually changed the dataset set.

Here the comparison is on sets, the function is pure, and the ledger is a
git-tracked CSV rather than a sheet a network failure can corrupt.
}
