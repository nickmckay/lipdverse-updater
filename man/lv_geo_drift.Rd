% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo.R
\name{lv_geo_drift}
\alias{lv_geo_drift}
\title{Coordinates that disagree between a QC sheet and the files}
\usage{
lv_geo_drift(cfg, backend, index = NULL)
}
\arguments{
\item{cfg}{From \code{\link[=lv_config]{lv_config()}}.}

\item{backend}{From \code{\link[=sheet_backend_google]{sheet_backend_google()}}.}

\item{index}{From \code{\link[=lv_db_index]{lv_db_index()}}.}
}
\value{
A tibble of the disagreements, with which side carries more precision
and whether one is the other rounded.
}
\description{
Written because a comparison bug hid these for as long as it existed:
\code{\link[=values_equal]{values_equal()}} used to compare numbers at the lesser of their two
precisions, so a coordinate rounded to one decimal matched the unrounded one
it came from and nothing could see the difference. Four hydroclimate2k
datasets lost precision that way.
}
\details{
Reports rather than repairs. Which side is right is a judgement — the finer
value usually is, but not when someone has corrected a coordinate outright.
}
