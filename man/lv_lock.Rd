% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lock.R
\name{lv_lock}
\alias{lv_lock}
\alias{lv_unlock}
\title{Acquire an exclusive lock on a compilation}
\usage{
lv_lock(compilation, run_id = lv_run_id(), timeout_minutes = 240)

lv_unlock(compilation)
}
\arguments{
\item{compilation}{Compilation name.}

\item{run_id}{Run identifier recorded in the lock.}

\item{timeout_minutes}{Age past which a lock is considered stale.}
}
\value{
The lock path, invisibly.
}
\description{
Uses \code{dir.create()}, which is atomic: exactly one caller wins the race.
}
\details{
Release with \code{\link[=lv_unlock]{lv_unlock()}}, but prefer \code{\link[=lv_locked]{lv_locked()}}, which registers the
release with \code{withr::defer()} so any \code{stop()}, \code{abort()} or interrupt still
unlocks. lipdverseR called \code{unFlagUpdate()} only on the last line of
\code{changeloggingAndUpdating()}, so any earlier failure left the database
flagged as updating indefinitely, and \code{addLipdToDatabase()} refused to run
until it was cleared by hand.
}
