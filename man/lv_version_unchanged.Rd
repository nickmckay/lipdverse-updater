% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/version.R
\name{lv_version_unchanged}
\alias{lv_version_unchanged}
\title{An unchanged version}
\usage{
lv_version_unchanged(prev, datasets = character())
}
\arguments{
\item{prev}{The current version string, from \code{\link[=lv_version_current]{lv_version_current()}}.}

\item{datasets}{The dataset set, unchanged.}
}
\value{
An \code{lv_version}.
}
\description{
The same shape \code{\link[=lv_tick_version]{lv_tick_version()}} returns, for a run that wrote nothing. A
run with an empty staging directory has not produced a new version, but every
consumer downstream still expects an \code{lv_version} -- and handing them the bare
string \code{\link[=lv_version_current]{lv_version_current()}} returns makes \code{ver$version} NULL, which then
travels silently into a sheet title or a ledger row.
}
