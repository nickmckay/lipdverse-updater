% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_store.R
\name{qc_store}
\alias{qc_store}
\title{The QC store}
\usage{
qc_store(path = lv_path("qcstore"))
}
\arguments{
\item{path}{Store root. Defaults to \code{\link[=lv_path]{lv_path()}}\code{("qcstore")}.}
}
\value{
An \code{lv_qc_store} object.
}
\description{
QC state is an append-only log of cell-level events. The current state is the
latest event per \verb{(tsid, field)}; the state at any past moment is the same
query with a time bound.
}
\details{
The store is the reason this rewrite exists. lipdverseR kept QC state only in
a Google Sheet, with \code{lastUpdate.csv} overwritten in place each run, so when
a merge dropped a curated value there was nothing to diff against and no way
to tell when it went.

Every event records \code{old_present} and \code{new_present} alongside the values.
That distinction — a cell that is absent versus a cell that holds nothing —
is what \code{daff} could not express, and why NA arriving from the files was read
as "delete this" and wiped curated fields.
}
\section{Layout}{

Text is the source of truth; git supplies the history.

\if{html}{\out{<div class="sourceCode">}}\preformatted{compilations/<compilation>/
  events/<timestamp>_<run_id>.csv   append-only cell deltas
  state.csv                         materialised current state
}\if{html}{\out{</div>}}
}

