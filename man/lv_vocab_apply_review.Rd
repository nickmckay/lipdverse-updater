% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab_review.R
\name{lv_vocab_apply_review}
\alias{lv_vocab_apply_review}
\title{Apply a completed vocabulary review}
\usage{
lv_vocab_apply_review(
  path,
  store = qc_store(),
  actor = lv_actor(),
  run_id = lv_run_id(),
  patch_dir = fs::path(store$path, "vocab", "patches"),
  dry_run = TRUE
)
}
\arguments{
\item{path}{A review file produced by \code{\link[=lv_vocab_review]{lv_vocab_review()}} and filled in.}

\item{store}{From \code{\link[=qc_store]{qc_store()}}.}

\item{actor}{Who decided; recorded with each row.}

\item{run_id}{Run identifier.}

\item{patch_dir}{Where to write the upstream patch files. \code{NULL} to skip.}

\item{dry_run}{Report without appending. Defaults to \code{TRUE}.}
}
\value{
A list of \code{decisions}, \code{patches} and \code{remap}, invisibly.
}
\description{
Appends the decisions to the store, and writes one patch file per affected
vocabulary in the schema of its Google Sheet, ready to be appended upstream.
\strong{Nothing is written to any Google Sheet}: the alignment sheets are shared and
authoritative, so pushing to them stays a deliberate human act.
}
