% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{lv_cached}
\alias{lv_cached}
\title{Content-addressed stage cache}
\usage{
lv_cached(stage, inputs, expr, version = 1L, use_cache = TRUE)
}
\arguments{
\item{stage}{Stage name.}

\item{inputs}{List of values the result depends on.}

\item{expr}{Expression producing the result; evaluated only on a miss.}

\item{version}{Stage version; bump to invalidate.}

\item{use_cache}{Set \code{FALSE} to force recomputation.}
}
\value{
The stage result.
}
\description{
Caches a stage's result under a key derived from the stage name, a
stage-version constant, and the hashes of its inputs. Bumping the version
constant in the calling code invalidates every entry for that stage.
}
\details{
This is drake's useful property without drake's coupling: lipdverseR's
stages communicated through a \code{params}/\code{data} list splatted into each
function's frame with \code{assign()}, so a target's real inputs were invisible
and cache correctness could not be reasoned about.
}
