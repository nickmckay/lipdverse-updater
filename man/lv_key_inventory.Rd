% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keys.R
\name{lv_key_inventory}
\alias{lv_key_inventory}
\title{Inventory every key present in a database}
\usage{
lv_key_inventory(dir = lv_path("database"), collapse_index = TRUE)
}
\arguments{
\item{dir}{Directory of \code{.lpd} files, or an \code{lv_scan}.}

\item{collapse_index}{Collapse \code{pubN_}/\code{interpretationN_} to 1.}
}
\value{
A tibble of \code{key}, \code{count}, \code{n_datasets}, \code{scope}, \code{example}.
}
\description{
Walks the metadata of every \code{.lpd} and counts how often each key occurs,
naming keys the way the LiPD timeseries flattening does
(\code{paleoData_variableName}, \code{geo_latitude}, \code{pub1_doi}) so the result can be
compared directly against a key-standardization mapping.
}
\details{
Publication and interpretation indices are collapsed to 1, matching the
convention in the standardization sheet: \code{pub3_doi} is the same key as
\code{pub1_doi}, occurring in a later slot.
}
