% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_merge.R
\name{qc_merge_rules}
\alias{qc_merge_rules}
\title{Three-way QC merge}
\description{
Merges three views of the same cells:
}
\details{
\describe{
\item{\code{base}}{What the store last recorded for this compilation.}
\item{\code{sheet}}{What the QC sheet says now.}
\item{\code{frame}}{What the LiPD files say now.}
}

Replaces \code{daff::merge_data()}, which compared whole rows and could not
express "absent" separately from "empty". NA arriving from the files read as
a deletion and wiped curated values; the surviving workarounds in lipdverseR
were an NA-backfill pass, a \code{sticky_fields} restore, and string-patching of
\code{"((( null ))) TRUE /// FALSE"} conflict markers.
}
\section{Rules}{

Per cell, with base \code{b}, sheet \code{s}, file \code{f}:\tabular{ll}{
   condition \tab result \cr
   \code{s == b}, \code{f == b} \tab unchanged \cr
   \code{s != b}, \code{f == b} \tab sheet (a curator edit) \cr
   \code{s == b}, \code{f != b} \tab file (a file change) \cr
   \code{s != b}, \code{f != b}, \code{s == f} \tab converged \cr
   \code{s != b}, \code{f != b}, \code{s != f} \tab by ownership \cr
}


On genuine divergence, \code{ownership} decides: \code{curator} takes the sheet,
\code{machine} takes the file, \code{key} is an error, and \code{shared} is a conflict —
base is retained and the disagreement reported rather than guessed.
}

\section{The clear rule}{

\strong{A cell is only cleared when the clear is explicit.} A blank arriving from
the files never deletes. A blank from the sheet deletes only where the
registry marks the field \code{nullable_by_curator}, or where the cell holds the
sentinel \verb{<<CLEAR>>}. Otherwise blank means "unchanged".

This single rule is what makes the NA-as-deletion loss impossible.

The one mirror on the file side is a \emph{calculated} field: \code{\link[=lv_calculate]{lv_calculate()}}
emits its cell whether or not the calculation produced anything, so a present
but empty cell on a \code{machine}-owned field states that the value no longer
exists and clears it. \code{\link[=qc_frame]{qc_frame()}} emits populated values only and cannot
produce that shape, so the rule reaches exactly the fields it is meant for.

A clear applies against whatever is displayed, not only against the stored
baseline. A legacy sheet holds values the store never had and the files never
had, and requiring a non-blank base made those unclearable in principle:
nothing computes them, so nothing could ever contradict them. Such a clear
also has to be reported as a \emph{change}, since base and result are both empty
and the ordinary comparison calls that "unchanged" -- and an unchanged cell
reaches neither the plan nor \code{\link[=qc_sheet_push]{qc_sheet_push()}}, which patches only what it is
told changed.
}

