% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab_review.R
\name{vocab_decisions}
\alias{vocab_decisions}
\alias{lv_vocab_decisions}
\title{Curator decisions on unrecognised vocabulary}
\usage{
lv_vocab_decisions(store = qc_store())
}
\arguments{
\item{store}{From \code{\link[=qc_store]{qc_store()}}.}
}
\value{
A tibble of recorded decisions; zero rows if none have been made.
}
\description{
The pinned tables in \code{inst/extdata/vocab} are a snapshot of
\code{standardTables.RDS}, which is itself built from seven Google Sheets listed in
the "LiPD-PaST alignment directory". Those sheets are the source of truth, and
this package does not write to them: a decision recorded here takes effect
locally at once, and is \emph{also} emitted in the schema of the target sheet so it
can be appended upstream deliberately, by a person, in one batch.
}
\details{
Decisions live in \code{vocab/decisions.csv} in the QC store, append-only and
git-tracked, so a value is reviewed once and never resurfaces.

Four decisions:

\describe{
\item{\code{synonym}}{the value means an existing \code{lipdName}. Overlaid onto the
pinned table, so \code{\link[=vocab_standardize]{vocab_standardize()}} matches it by the \code{synonym} rule.}
\item{\code{new_term}}{the vocabulary lacks a term this value needs. With no
\code{map_to} the value itself becomes canonical. With a \code{map_to}, that name is
created instead and the value maps onto it, which is what you want when
the value is not a good term: \verb{Annual HHI} should not become a term, but
\code{moistureIndex} should, with \verb{Annual HHI} pointing at it. Add
\code{also_field} and \code{also_value} as well and the value decomposes onto the
new term, so \verb{Annual HHI} becomes \code{moistureIndex} plus a seasonality of
\code{Annual} rather than baking the season into the name.}
\item{\code{decompose}}{the value carries two facts at once, e.g.
\verb{MJJASO precip index} is \code{precipitation} \emph{plus} a seasonality of \code{MJJASO}.
Deliberately \strong{not} overlaid as a synonym: a synonym would rewrite the
variable name and silently drop the season. Applied only by
\code{\link[=lv_ingest_standardize]{lv_ingest_standardize()}}, which writes both fields together.}
\item{\code{leave}}{correct as it stands, or not yet decidable. Recorded so it is
not offered again.}
}
}
