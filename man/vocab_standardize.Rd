% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab.R
\name{vocab_standardize}
\alias{vocab_standardize}
\title{Standardize values against a controlled vocabulary}
\usage{
vocab_standardize(x, key, vocab = lv_vocab())
}
\arguments{
\item{x}{Character vector of values.}

\item{key}{Vocabulary key, e.g. \code{"archiveType"}.}

\item{vocab}{From \code{\link[=lv_vocab]{lv_vocab()}}.}
}
\value{
A tibble of \code{input}, \code{value}, \code{matched}, \code{rule}.
}
\description{
A pure function over a character vector, so it can be tested and so a run can
report what it \emph{would} change before changing it. \code{standardizeValue()} in
lipdverseR conflates detection with mutation, which is why that path resists
testing; and \code{standardizeLipdBatch()} reaches the values by round-tripping the
whole dataset through \code{as.lipdTsTibble()}/\code{as.lipd()}, which fabricates an
empty interpretation on every column that has fewer than the dataset's
maximum. Nothing here touches structure.
}
\details{
Matching is tried in order, most exact first, and the rule is reported:

\describe{
\item{\code{canonical}}{already the standard name}
\item{\code{synonym}}{an exact listed synonym}
\item{\code{case}}{differs only in case}
\item{\code{trim}}{differs only in surrounding whitespace}
\item{\code{none}}{no match; the value is left alone and reported}
}
}
