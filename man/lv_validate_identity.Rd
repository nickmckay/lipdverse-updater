% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_identity.R
\name{lv_validate_identity}
\alias{lv_validate_identity}
\title{Validate database identity}
\usage{
lv_validate_identity(index, strict = TRUE)
}
\arguments{
\item{index}{An \code{lv_index} from \code{\link[=lv_db_index]{lv_db_index()}}.}

\item{strict}{Treat duplicate identifiers as errors rather than warnings.}
}
\value{
An \code{lv_issues} tibble.
}
\description{
Runs before anything else in an update. Every finding is an \code{lv_issues} row;
\code{error} severity aborts the run.
}
\details{
Nothing is auto-renamed. lipdverseR resolved duplicate TSids by appending
\code{-dup} in a \code{while} loop (\code{standardizeQCInfo:759}, \code{createQcFromFile:817}),
which mints identifiers that match nothing downstream and hides the problem
rather than fixing it. Repair here is a separate, explicit act.
}
