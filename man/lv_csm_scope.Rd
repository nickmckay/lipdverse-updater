% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csm.R
\name{lv_csm_scope}
\alias{lv_csm_scope}
\title{Keep only the csm a compilation may speak for}
\usage{
lv_csm_scope(cells, compilation, registry = lv_qc_fields())
}
\arguments{
\item{cells}{A cell table.}

\item{compilation}{Compilation name.}

\item{registry}{Field registry.}
}
\value{
A list of \code{cells} to merge and \code{foreign} cells held back.
}
\description{
Splits a sheet's cells into those this compilation owns and those belonging to
another compilation's \code{csm}. Merged fields and everything else pass through
untouched; only role \code{csm} is scoped.
}
\details{
Apply this to the sheet before merging, never to the store baseline: see the
scoping note in \link{csm}.
}
