% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identity.R
\name{lv_db_index}
\alias{lv_db_index}
\title{Build an identity index over the database}
\usage{
lv_db_index(scan = lv_scan(), workers = NULL, cache = TRUE)
}
\arguments{
\item{scan}{An \code{lv_scan}, or a directory path.}

\item{workers}{Parallel workers.}

\item{cache}{Reuse cached per-file extracts keyed by file md5.}
}
\value{
An \code{lv_index} list with \code{datasets} and \code{timeseries} tibbles.
}
\description{
Reads only the metadata JSON inside each \code{.lpd} (a zip) rather than doing a
full \code{\link[lipdR:readLipd]{lipdR::readLipd()}}, because identity checks need names and IDs, not
data columns. That makes indexing the whole database cheap enough to run
before every update.
}
\details{
Replaces lipdverseR's \code{createDatabaseReference()}, which cached into a global
via \verb{<<-} and whose duplicate-name branch tested \code{ref$dataSetNames}, a column
that does not exist, so \code{duplicated(NULL)} was always \code{logical(0)} and the
warning could never fire.
}
