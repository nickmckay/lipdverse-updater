% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab.R
\name{lv_audit_vocabulary}
\alias{lv_audit_vocabulary}
\title{Audit the whole QC sheet against the controlled vocabularies}
\usage{
lv_audit_vocabulary(
  sheet,
  base = NULL,
  frame = NULL,
  registry = lv_qc_fields(),
  vocab = lv_vocab_overlay(),
  index = NULL
)
}
\arguments{
\item{sheet}{Long cell tibble from \code{\link[=qc_sheet_pull]{qc_sheet_pull()}}.}

\item{base}{Long cell tibble from \code{\link[=qc_state_current]{qc_state_current()}}. Optional.}

\item{frame}{Long cell tibble from \code{\link[=qc_frame]{qc_frame()}}. Optional.}

\item{registry}{Field registry.}

\item{vocab}{Vocabulary tables, overlay included.}

\item{index}{Database index, used to name datasets.}
}
\value{
One row per field, value and disposition, commonest first, with
\code{datasets} as a list column.
}
\description{
\code{\link[=lv_check_vocabulary]{lv_check_vocabulary()}} runs on the cells a merge is about to write, which is
the right scope for catching a curator typing something new: that value wins
and reaches the files. It is the wrong scope for finding what is already
wrong. A bad value the sheet and the store both hold is not a change, so it
never appears in the plan, and no merge-time check will ever see it.
}
\details{
hydroclimate2k carried \verb{diatoms from lake core}, \code{w} and three others across
16 rows that way. Nothing surfaced them until the sheet was read end to end.

So this reads every cell, and then says what the run will do about each one,
which is the part that decides whether it needs attention:

\describe{
\item{\verb{the files will correct it}}{The files hold a different, valid value
and the sheet has not moved from the baseline, so the files win and the
sheet is corrected on push. Nothing to do.}
\item{\verb{in the files too}}{The files hold the same bad value. The run changes
nothing, and this is the case that needs a real decision -- either a
vocabulary entry or a correction at the source.}
\item{\verb{the sheet has moved}}{The sheet differs from both the baseline and
the files. Ownership decides, so read the plan: for a curator-owned field
this value reaches the files.}
\item{\verb{no value in the files}}{Nothing on the file side to correct it.}
}

Reports; never blocks. Intended as a pre-flight, before the merge.
}
