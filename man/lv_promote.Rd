% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_write.R
\name{lv_promote}
\alias{lv_promote}
\title{Promote a directory of prepared LiPD files into the database}
\usage{
lv_promote(
  staging,
  dir = lv_path("database"),
  run_id = lv_run_id(),
  dry_run = TRUE,
  verify = TRUE,
  workers = NULL,
  allow_delete = FALSE,
  partial = FALSE,
  delete = character()
)
}
\arguments{
\item{staging}{Directory of prepared \code{.lpd} files.}

\item{dir}{Target database directory.}

\item{run_id}{Run identifier.}

\item{dry_run}{Verify and report, changing nothing.}

\item{verify}{Re-read every staged file before committing. Leave on.}

\item{workers}{Parallel verification workers.}

\item{allow_delete}{Permit removing live files absent from staging.}

\item{partial}{Staging holds only some of the database. Deletions are not
considered at all, so the run can add and replace without the files it
simply did not touch looking like removals. This is the normal case for an
incremental update; leave \code{FALSE} when staging is a whole database.}
}
\value{
An \code{lv_write_receipt}, invisibly.
}
\description{
Used when the files already exist — a migration, or a run that staged
elsewhere. \code{lv_write()} is the same commit machinery with a \code{writeLipd()}
step in front.
}
\details{
Staged files are \strong{moved} into place, not copied, so \code{staging} is empty on
return and cannot be promoted a second time. The undo path is \code{.trash}, via
\code{\link[=lv_write_rollback]{lv_write_rollback()}}, not the staging directory.
}
