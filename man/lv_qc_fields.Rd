% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{lv_qc_fields}
\alias{lv_qc_fields}
\title{The QC field registry}
\usage{
lv_qc_fields(validate = TRUE)
}
\arguments{
\item{validate}{Check the registry before returning it.}
}
\value{
A tibble.
}
\description{
One row per QC sheet column, recording how the merge engine should treat it.
Built from the reviewed decisions in \verb{review/} by
\code{data-raw/build_qc_fields.R}; edit those and rebuild rather than editing the
registry directly.
}
\details{
\code{role} says what kind of field it is:

\describe{
\item{\code{merged}}{Participates in the three-way merge under \code{ownership}.}
\item{\code{csm}}{Compilation-specific; lives under \code{csm} in the matching
\code{inCompilation} entry, not in the shared namespace.}
\item{\code{csm_pending}}{Destined for \code{csm}, target compilation not yet decided.}
\item{\code{key}}{Identifier. Never merged; disagreement is an error.}
\item{\code{synonym}}{An alias; \code{canonical} names the field it resolves to.}
\item{\code{control}}{An instruction column consumed by the pipeline.}
\item{\code{unused}}{Present in a sheet but never populated.}
\item{\code{delete}}{To be removed from the files.}
}

For \code{merged} fields, \code{ownership} decides who wins a disagreement:
\code{curator} (the sheet), \code{machine} (the file), \code{shared} (a real conflict,
retain base and report), or \code{key}.
}
