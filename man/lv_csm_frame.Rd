% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csm.R
\name{lv_csm_frame}
\alias{lv_csm_frame}
\title{The file-side view of a compilation's csm}
\usage{
lv_csm_frame(
  dir = lv_path("database"),
  compilation,
  registry = lv_qc_fields(),
  datasets = NULL,
  tsids = NULL,
  progress = TRUE
)
}
\arguments{
\item{dir}{Directory of \code{.lpd} files, or an \code{lv_scan}.}

\item{compilation}{Compilation name.}

\item{registry}{Field registry.}

\item{datasets}{Optional dataset names to restrict to.}

\item{tsids}{Optional TSids to restrict to.}

\item{progress}{Show progress.}
}
\value{
A cell table.
}
\description{
Reads \code{inCompilation[[compilation]]$csm} from each file and emits the same
long cell table \code{\link[=qc_frame]{qc_frame()}} and \code{\link[=qc_sheet_pull]{qc_sheet_pull()}} produce, so csm merges by
the ordinary three-way rules.
}
\details{
Only populated values are emitted. An absent cell means "unchanged", which is
what keeps a file that has never carried a csm value from reading as a
deletion of one the curator just typed.
}
