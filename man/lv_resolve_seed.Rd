% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_compilation.R
\name{lv_resolve_seed}
\alias{lv_resolve_seed}
\title{Resolve a compilation seed to TSids}
\usage{
lv_resolve_seed(seed, index, by, scope = c("qc", "all"))
}
\arguments{
\item{seed}{Character vector of TSids, datasetIds or dataSetNames.}

\item{index}{An \code{lv_index} from \code{\link[=lv_db_index]{lv_db_index()}}.}

\item{by}{Which of those the seed holds. Required; see \code{\link[=lv_seed_kind]{lv_seed_kind()}}.}

\item{scope}{For dataset-level seeds: \code{"qc"} for the QC scope, \code{"all"} for
every timeseries in the dataset.}
}
\value{
A list of \code{tsids}, the \code{by} used, the \code{matched} and \code{unmatched} seed
values, and an \code{lv_issues} tibble.
}
\description{
A compilation usually starts as a list someone has to hand, and that list is
as likely to be dataset names or datasetIds as TSids. This resolves any of
them to the TSids that membership is actually written against.
}
\details{
\code{by} is required. Detecting the kind would mean deciding, on the caller's
behalf, what a list of strings means, and a wrong guess does not fail: it
produces a plausible compilation of the wrong things. \code{\link[=lv_seed_kind]{lv_seed_kind()}}
answers the question when it is genuinely unknown, and reports rather than
acts.

Values matching nothing are reported rather than dropped. A half-matching
list is the dangerous case, where the compilation comes out smaller than
intended and nothing says so.

For a dataset-level seed the QC scope is used: paleo measurement columns,
axes excluded. A compilation of \code{year} and \code{depth} columns is not meaningful,
and including them would put them in the QC sheet, which is the thing the
hydroclimate2k curators asked to have removed. \code{scope = "all"} overrides.
}
