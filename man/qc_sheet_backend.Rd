% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_sheet.R
\name{qc_sheet_backend}
\alias{qc_sheet_backend}
\alias{sheet_backend_google}
\alias{sheet_backend_local}
\title{QC sheet backends}
\usage{
sheet_backend_google(
  email = Sys.getenv("LIPDVERSE_GOOG_EMAIL", "nick.mckay2@gmail.com"),
  token_dir = Sys.getenv("LIPDVERSE_GOOG_CACHE",
    path.expand("~/GitHub/lipdverseR/.secret"))
)

sheet_backend_local(dir)
}
\arguments{
\item{email}{Google account.}

\item{token_dir}{Credential cache directory.}

\item{dir}{Directory of \verb{<sheet_id>/<tab>.csv} files.}
}
\description{
All sheet access goes through a backend so the pipeline can be tested
without a network, and so a shadow run can operate on captured sheets rather
than the live ones.
}
\details{
Two hardening rules apply to every read, both from failures in lipdverseR:
\itemize{
\item \strong{Tabs are addressed by name, never index.} \code{sheet = 1} read whatever tab
happened to be first, so reordering tabs silently changed which data was
loaded.
\item \strong{Every column is read as character.} Type inference looked only at the
first rows, so a sparse column whose first value appeared past row 1000 was
typed logical and its character values were dropped.
}
}
