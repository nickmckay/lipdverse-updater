% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_store.R
\name{lv_qc_retire}
\alias{lv_qc_retire}
\title{Retire cells from a compilation's QC state}
\usage{
lv_qc_retire(
  store,
  compilation,
  tsids,
  reason,
  actor = lv_actor(),
  run_id = lv_run_id(),
  dry_run = TRUE
)
}
\arguments{
\item{store}{From \code{\link[=qc_store]{qc_store()}}.}

\item{compilation}{Compilation name.}

\item{tsids}{Timeseries to retire. All their cells are tombstoned.}

\item{reason}{Recorded on every event.}

\item{actor}{Who did it.}

\item{run_id}{Run identifier.}

\item{dry_run}{Report without appending. Defaults to \code{TRUE}.}
}
\value{
The events appended (or that would be), invisibly.
}
\description{
Appends tombstones rather than deleting anything: the store is append-only,
so the history of a retired cell stays readable and the retirement itself is
an event with a reason attached.
}
\details{
Written for a specific mess. The hydroclimate2k baseline was seeded with 6,495
chronData timeseries, each carrying a lone \code{inThisCompilation} flag and no
dataSetName or archiveType, because the seeder took its scope straight off the
index. Fixing the scope stops more arriving; it does not remove what was
already recorded, and the merge would keep re-surfacing them.
}
