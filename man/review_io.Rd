% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_io.R
\name{review_io}
\alias{review_io}
\alias{lv_review_read}
\alias{lv_review_write}
\alias{lv_review_export_csv}
\alias{lv_review_empty}
\alias{lv_review_convert}
\title{Read and write vocabulary review files}
\usage{
lv_review_read(path)

lv_review_write(r, path, meta = attr(r, "meta"))

lv_review_export_csv(r, path)

lv_review_empty()

lv_review_convert(path, out = sub("\\\\.csv$", ".json", path))
}
\arguments{
\item{path}{A \code{.json} (or legacy \code{.csv}) review file.}

\item{r}{A review tibble.}

\item{meta}{Named list written alongside the items. Defaults to whatever
\code{r} carries, so a round trip preserves it.}

\item{out}{Where to write the converted file. Defaults to the same path with
a \code{.json} extension.}
}
\value{
A tibble of one row per value, with \code{datasets}, \code{candidates},
\code{source_pdfs}, \code{examples}, \code{siblings} and \code{past_candidates} as list columns,
and a \code{meta} attribute. \code{examples} and \code{siblings} are absent from files
written before they existed, and read back empty rather than failing.
}
\description{
JSON is canonical. A review is not really a table: \code{datasets}, \code{candidates}
and the PaST matches are lists, the PaST matches are records with their own
fields, and every value carries two parallel sets of the same six fields, one
proposed and one decided. Flattened into CSV all of that becomes pipe-joined
strings and \code{proposed_}-prefixed columns, which is ambiguous exactly where it
matters: a \code{|} inside a rationale is indistinguishable from a separator.
}
\details{
In JSON they are arrays and nested objects, and \code{null} means undecided rather
than an empty cell that might be an empty string.

CSV is still read, so review files written before the switch are not
stranded, but it is never written.
}
