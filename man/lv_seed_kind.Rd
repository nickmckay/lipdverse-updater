% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_compilation.R
\name{lv_seed_kind}
\alias{lv_seed_kind}
\title{What kind of identifiers does a list hold?}
\usage{
lv_seed_kind(seed, index)
}
\arguments{
\item{seed}{Character vector.}

\item{index}{An \code{lv_index} from \code{\link[=lv_db_index]{lv_db_index()}}.}
}
\value{
A tibble of one row per kind, with the match count and share.
}
\description{
Reports how many of the values match as TSids, datasetIds and dataSetNames,
so \code{\link[=lv_resolve_seed]{lv_resolve_seed()}} can be told which it is rather than guessing. Answers
the question; does not act on the answer.
}
\details{
A list that matches two kinds is worth knowing about before it is used: it
usually means the list is mixed, or that it was pasted from two sources.
}
