% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_compilation.R
\name{lv_datasets_created_by}
\alias{lv_datasets_created_by}
\title{Datasets a compilation brought into LiPDverse}
\usage{
lv_datasets_created_by(names, compilation, dir = lv_path("database"))
}
\arguments{
\item{names}{Dataset names to check. Keep it small; this reads files.}

\item{compilation}{Compilation name.}

\item{dir}{Database directory.}
}
\value{
The subset of \code{names} whose files carry \code{createdBy} for this
compilation.
}
\description{
The ingest stamps \code{createdBy} on every column it mints for a compilation, so
the files themselves record which compilation a dataset arrived for. That is
the durable signal: a run receipt covers one batch, and the question outlives
the batch.
}
\details{
Used to catch datasets ingested for a compilation that never reached its
\code{datasetsInCompilation} tab. Until they do, nobody can curate them: the QC
sheet is scoped to that tab, so an ingest that stops at the files leaves the
data invisible to the people it was ingested for.
}
