% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab.R
\name{lv_vocab}
\alias{lv_vocab}
\alias{lv_vocab_pin}
\title{The pinned LiPDverse vocabulary}
\usage{
lv_vocab(validate = TRUE)

lv_vocab_pin()
}
\arguments{
\item{validate}{Check the tables before returning them.}
}
\value{
A named list of tibbles, with a \code{pin} attribute.
}
\description{
Read from \code{inst/extdata/vocab}, never from the network. \code{getStandardTables()}
in lipdverseR fetches \code{standardTables.RDS} over HTTP every time it is called,
so two runs a month apart are not comparable and an old run cannot be
reproduced at all. Refresh deliberately with \code{scripts/pin-vocabulary.R}.
}
\details{
Each table is one row per \verb{(lipdName, synonym)} pair: \code{archiveType} has 53
rows for 17 archive types.
}
