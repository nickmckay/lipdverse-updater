% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shadow_run.R
\name{shadow_run_legacy}
\alias{shadow_run_legacy}
\title{Run lipdverseR's pipeline against an isolated copy of the database}
\usage{
shadow_run_legacy(
  compilation,
  lipd_dir,
  lipdverse_r = "~/GitHub/lipdverseR",
  web_dir = fs::path(tempdir(), "shadow-web"),
  timeout = 6 * 3600,
  allow_live = FALSE
)
}
\arguments{
\item{compilation}{Compilation name.}

\item{lipd_dir}{Database copy to operate on. Must not be the live database.}

\item{lipdverse_r}{Path to the lipdverseR source.}

\item{web_dir}{Output directory for the legacy web stages.}

\item{timeout}{Seconds before the child session is killed.}

\item{allow_live}{Set \code{TRUE} to permit running against the real database.}
}
\value{
A list with the child's status and log path.
}
\description{
Runs in a separate R session via \code{callr}, because lipdverseR calls
\code{setwd()}, assigns into the global environment with \verb{<<-}, and caches Google
credentials relative to its working directory. None of that may leak into
the session driving the comparison.
}
\details{
\strong{This writes to Google Sheets} for the compilation it is given: the legacy
pipeline pushes merged QC back as part of a normal run. Use a throwaway QC
sheet, or accept that the real one will be written.

Not exercised by the test suite — it needs a full lipdverseR install, a live
Google session and hours of runtime. Treat the first use as exploratory.
}
