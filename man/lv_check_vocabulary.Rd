% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab.R
\name{lv_check_vocabulary}
\alias{lv_check_vocabulary}
\title{Check the values a run is about to write against the vocabulary}
\usage{
lv_check_vocabulary(
  cells,
  registry = lv_qc_fields(),
  vocab = lv_vocab_overlay(),
  index = NULL
)
}
\arguments{
\item{cells}{Cells about to be written, from the merge plan.}

\item{registry}{Field registry.}

\item{vocab}{From \code{\link[=lv_vocab_overlay]{lv_vocab_overlay()}}.}

\item{index}{From \code{\link[=lv_db_index]{lv_db_index()}}; supplies the dataset each TSid belongs to.}
}
\value{
An \code{lv_issues} tibble, one row per offending cell.
}
\description{
Vocabulary was only ever enforced at ingest. Nothing looked at values arriving
from a QC sheet, so \code{archiveType} drifted to \verb{diatoms from lake core}, \code{w} and
\code{Pollen} without anything noticing — a curator asked whether archiveType was
a controlled vocabulary at all, which is a fair question when nothing enforces
it.
}
\details{
Reports rather than blocks. A run that has already merged several hundred
datasets should not abort over one cell, and the cell is a curator's to
resolve, not a run's to overwrite. The unmatched values come back as issues
and go into a review file, the same shape \code{\link[=lv_vocab_review]{lv_vocab_review()}} produces for
ingest, so the decisions land in the same store and are asked once.
}
