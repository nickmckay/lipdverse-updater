% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csm.R
\name{lv_apply_csm}
\alias{lv_apply_csm}
\title{Apply merged csm cells to the files}
\usage{
lv_apply_csm(
  cells,
  index,
  compilation,
  dir = lv_path("database"),
  out,
  registry = lv_qc_fields(),
  progress = TRUE
)
}
\arguments{
\item{cells}{Merged cells; anything that is not a csm field of \code{compilation}
is reported and skipped.}

\item{index}{An \code{lv_index}.}

\item{compilation}{Compilation name.}

\item{dir}{Source database directory.}

\item{out}{Staging directory.}

\item{registry}{Field registry.}

\item{progress}{Show progress.}
}
\value{
A list of \code{n} applied, \code{issues} and \code{datasets} touched.
}
\description{
Writes each value into the compilation's own \code{inCompilation} entry, on the
column the TSid names. Written to staging for \code{\link[=lv_promote]{lv_promote()}}, never in place.
}
\details{
A cell whose column carries no entry for this compilation is not applied: csm
is metadata \emph{about a membership}, so without the membership there is nowhere
for it to live. Run \code{\link[=lv_apply_membership]{lv_apply_membership()}} first and point \code{index} at its
staging output, which is what the runner does.
}
