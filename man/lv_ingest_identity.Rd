% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{lv_ingest_identity}
\alias{lv_ingest_identity}
\title{Decide what to do with every incoming TSid}
\usage{
lv_ingest_identity(scan, index)
}
\arguments{
\item{scan}{From \code{\link[=lv_ingest_scan]{lv_ingest_scan()}}.}

\item{index}{An \code{lv_index} of the live database.}
}
\value{
\code{scan} with \code{action} (\code{keep}, \code{mint}, \code{remint}), \code{reason}, and the
resolved \code{new_TSid}.
}
\description{
A TSid that already exists in LiPDverse means one of two opposite things, and
the difference decides whether it is preserved or replaced:
}
\details{
\itemize{
\item The file \strong{is} that dataset — its \code{datasetId} matches, or it has no
\code{datasetId} yet and its \code{dataSetName} matches. This is an update, and the
TSid must be kept so the timeseries keeps its identity and its compilation
memberships.
\item The file is \strong{not} that dataset. Then someone has used an existing file as
a template and left its identifiers in place. The TSid is re-minted.
}

The same reasoning applies inside a batch: a TSid repeated across submissions
that no existing dataset claims is a copied template, and every occurrence is
re-minted rather than one being arbitrarily blessed.
}
