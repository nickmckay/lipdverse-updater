% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{lv_value_hashes}
\alias{lv_value_hashes}
\title{Hash the measurement values of every dataset in a directory}
\usage{
lv_value_hashes(dir, cache = NULL, progress = TRUE)
}
\arguments{
\item{dir}{Directory of \code{.lpd} files.}

\item{cache}{Cache file, keyed on the directory fingerprint.}

\item{progress}{Show progress.}
}
\value{
A named list of md5 vectors, one entry per dataset.
}
\description{
The only signal that reliably distinguishes "the same record under a
different name" from "a different record at the same site". Metadata does
not: variable-name overlap is 1.00 for three unrelated pollen studies at one
site, and 622 coordinate clusters in the database cover 1,441 datasets
because a site has many cores.
}
