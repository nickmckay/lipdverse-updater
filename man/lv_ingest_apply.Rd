% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{lv_ingest_apply}
\alias{lv_ingest_apply}
\title{Write the resolved identity into staged copies of the incoming files}
\usage{
lv_ingest_apply(plan, dir, out, index, compilation = NULL, progress = TRUE)
}
\arguments{
\item{plan}{From \code{\link[=lv_ingest_identity]{lv_ingest_identity()}}.}

\item{dir}{Directory holding the incoming files.}

\item{out}{Staging directory.}

\item{index}{An \code{lv_index}, so a minted \code{datasetId} cannot collide.}

\item{progress}{Show progress.}
}
\value{
A list of \code{staged} files, \code{skipped}, and \code{issues}.
}
\description{
Never writes in place. Assigns the TSids from \code{\link[=lv_ingest_identity]{lv_ingest_identity()}} and a
\code{datasetId} to any file lacking one, then stages for \code{\link[=lv_promote]{lv_promote()}}.
}
\details{
Columns are matched by position within a deterministic walk, because
\code{variableName} is not unique inside a table -- one incoming file carries four
\code{d13C} columns and five \code{d2H}. The walk is verified against the plan before
anything is assigned, so a file whose structure does not line up is reported
and skipped rather than silently mis-assigned.
}
