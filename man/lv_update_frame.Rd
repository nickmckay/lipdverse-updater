% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update.R
\name{lv_update_frame}
\alias{lv_update_frame}
\title{Run one compilation through the whole pipeline}
\usage{
lv_update_frame(db, comp, ds, ts, calcs, progress = FALSE)
}
\arguments{
\item{progress}{Print the running report.}

\item{compilation}{Compilation name.}

\item{commit}{Write: promote the staged files, append to the store, push the
sheet. \code{FALSE} reports without writing anything.}

\item{cfg}{An \code{lv_config}; defaults to the compilation's.}

\item{dir}{Database directory.}

\item{store}{A QC store.}

\item{backend}{A sheet backend.}

\item{stage}{Staging directory; a scratch path under \verb{~/lipdverse-staging} by
default. Emptied at the start of the run.}

\item{run_id}{Run id. A run id names one write; never reuse one.}

\item{patch}{Force a patch push even when there are new rows, which keeps the
tab's formatting and appends the new rows unformatted at the bottom.}

\item{snapshot}{Take a database snapshot before promoting. Only consulted
when \code{commit}.}

\item{export}{Write the canonical export. Only consulted when \code{commit}.}

\item{rename_sheet}{Retitle the QC sheet with the new version. Only consulted
when \code{commit}.}
}
\value{
An \code{lv_update_result}.
}
\description{
scan -> frame -> sheet pull -> merge -> store -> apply -> stage -> promote,
then the two invariants that hold regardless of what the legacy pipeline did:
}
\details{
\describe{
\item{idempotence}{a second run against unchanged inputs changes nothing}
\item{no collateral change}{files outside the compilation are untouched}
}

Both are asserted rather than eyeballed, because both have already caught real
bugs that field-by-field diffs reported as clean.

This lived in \code{scripts/run-compilation.R} until 2026-08-12, which meant the
most consequential code in the package -- merge, apply, promote, version,
changelog, export, and both invariants -- was the only part no test ever
touched, and the shadow harness could not call it at all.
}
\section{Nothing is written unless \code{commit}}{

A dry run reads the sheet, reads the files, merges, and stages into a
scratch directory, then reports what it would do. It writes nothing to the
database, the store, or the sheet. That is the mode to use for anything
unattended.
}

