% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_write.R
\name{lv_run_receipt}
\alias{lv_run_receipt}
\title{What a committed run did}
\usage{
lv_run_receipt(run_id = NULL, dir = lv_path("database"))
}
\arguments{
\item{run_id}{Run identifier; defaults to the most recent run.}

\item{dir}{Database directory.}
}
\value{
A tibble of \code{file}, \code{action}, \code{md5_old}, \code{md5_new}, with \code{run_id},
\code{at} and \code{trash} attributes.
}
\description{
Reads a run receipt back. Staging is emptied by the move into place, so after
a promote the receipt is the only record of which datasets were added as
opposed to replaced -- and that is exactly what the next step needs, since
new datasets have to be offered to a compilation and updated ones do not.
}
