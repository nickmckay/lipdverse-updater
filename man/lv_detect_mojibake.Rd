% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mojibake.R
\name{lv_detect_mojibake}
\alias{lv_detect_mojibake}
\alias{lv_repair_mojibake}
\title{Detect and repair mojibake}
\usage{
lv_detect_mojibake(x, encodings = c("macintosh", "CP1252"))

lv_repair_mojibake(cfg, backend, tab = cfg$qc_tabs$qc, dry_run = TRUE)
}
\arguments{
\item{x}{A character vector.}

\item{encodings}{Encodings to try, in order.}

\item{cfg}{From \code{\link[=lv_config]{lv_config()}}.}

\item{backend}{From \code{\link[=sheet_backend_google]{sheet_backend_google()}}.}

\item{tab}{Tab to repair. Defaults to the QC tab.}

\item{dry_run}{Report without writing. Defaults to \code{TRUE}.}
}
\value{
A tibble of \code{input}, \code{repaired}, \code{is_mojibake} and \code{encoding}.

A tibble of the cells repaired (or that would be), invisibly.
}
\description{
Mojibake here is one specific accident: UTF-8 bytes decoded as Mac OS Roman.
An en-dash, \verb{e2 80 93}, becomes \verb{‚Äì}; a delta, \verb{ce b4}, becomes \verb{Œ¥}. It
entered the LiPDverse QC sheets somewhere upstream and reaches the \code{.lpd}
files from there.
}
\details{
Detection is by round trip rather than by looking for suspicious characters,
which matters because the suspicious characters are also ordinary ones:
\code{Ã}, \code{â} and \code{Â} appear in perfectly good Portuguese, French and Welsh. A
string is mojibake only if re-encoding it to Mac OS Roman yields bytes that
are \emph{valid UTF-8 and decode to something different}. \code{São} fails that test,
because \code{ã} is a single Mac Roman byte that is not valid UTF-8 on its own, so
it is left alone. \verb{Œ¥} passes, because those two bytes are \verb{ce b4}.

The repair is therefore exact rather than a guess, and reversible in
principle, though there is no reason to reverse it.

Two encodings are tried, because both accidents occur: Mac OS Roman is what
the LiPDverse sheets carry (\verb{Œ¥}, \verb{‚Äì}), and Windows-1252 is the commoner
variety everywhere else (\verb{Ã©}, \verb{BÃ¼ntgen}). Mac Roman is tried first, since
that is what these sheets have, and the first encoding that round trips wins.
}
