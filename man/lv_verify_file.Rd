% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_write.R
\name{lv_verify_file}
\alias{lv_verify_file}
\title{Verify a staged LiPD file is readable and intact}
\usage{
lv_verify_file(path, expect_name = NULL, min_bytes = 200, strict_valid = TRUE)
}
\arguments{
\item{path}{Staged file.}

\item{expect_name}{Expected dataSetName, if known.}

\item{min_bytes}{Reject implausibly small files.}

\item{strict_valid}{Treat a \code{validLipd()} failure as an error. \code{validLipd()}
signals by return value rather than by condition, so this must be checked
explicitly.}
}
\value{
An \code{lv_issues} tibble; empty means the file passed.
}
\description{
Re-reads from disk rather than trusting the object that was written. A file
that cannot be read back is not a file that may replace a good one.
}
