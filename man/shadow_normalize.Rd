% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shadow.R
\name{shadow_normalize}
\alias{shadow_normalize}
\title{Flatten a database directory to canonical long form}
\usage{
shadow_normalize(dir, ignore = shadow_ignore(), progress = TRUE)
}
\arguments{
\item{dir}{Directory of \code{.lpd} files, or an \code{lv_scan}.}

\item{ignore}{Tibble of ignore patterns; \code{NULL} keeps everything.}

\item{progress}{Show a progress bar.}
}
\value{
A tibble: \code{dataSetName}, \code{datasetId}, \code{TSid}, \code{field_path}, \code{value}.
}
\description{
Produces one row per leaf value, keyed by \code{datasetId}, \code{TSid} and
\code{field_path}, with deterministic ordering. Two directories normalized this
way can be compared field by field regardless of how their files are
ordered internally.
}
\details{
Columns are keyed by \strong{TSid, not position}, so reordering columns within a
table produces no diff. Data vectors are replaced by a hash of their
contents: a change is still detected, but the output stays small enough to
diff 7,000 datasets.
}
