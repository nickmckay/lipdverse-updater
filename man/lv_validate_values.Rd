% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_merge.R
\name{lv_validate_values}
\alias{lv_validate_values}
\title{Check resolved values against what the registry says they are}
\usage{
lv_validate_values(cells, registry = lv_qc_fields())
}
\arguments{
\item{cells}{Resolved cells (\code{tsid}, \code{field}, \code{value}).}

\item{registry}{Field registry.}
}
\value{
An \code{lv_issues} tibble.
}
\description{
A curator can type anything into a sheet cell. Without this, a value the
files cannot legally hold reaches \code{\link[=lv_apply_qc]{lv_apply_qc()}}, gets written into staging,
and is caught only by the writer's verification -- which aborts the whole
run. On hydroclimate2k one cell held a URL pasted into the latitude column,
and it would have blocked a 413-file promote.
}
\details{
Reporting per cell instead lets the run continue with the other 2,738
changes and hands the curator a specific TSid and field to fix.
}
