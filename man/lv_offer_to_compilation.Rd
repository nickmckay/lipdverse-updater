% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_compilation.R
\name{lv_offer_to_compilation}
\alias{lv_offer_to_compilation}
\title{Offer datasets to a compilation}
\usage{
lv_offer_to_compilation(
  names,
  cfg,
  backend,
  index = NULL,
  in_compilation = TRUE,
  dry_run = TRUE
)
}
\arguments{
\item{names}{Dataset names. Typically the \code{add} rows of \code{\link[=lv_run_receipt]{lv_run_receipt()}}.}

\item{cfg}{From \code{\link[=lv_config]{lv_config()}}.}

\item{backend}{From \code{\link[=sheet_backend_google]{sheet_backend_google()}}.}

\item{index}{From \code{\link[=lv_db_index]{lv_db_index()}}; supplies each dataset's id.}

\item{in_compilation}{Value for the \code{inComp} column. \code{TRUE} marks them as
considered, which is what makes them appear in the QC tab.}

\item{dry_run}{Report without writing. Defaults to \code{TRUE}.}
}
\value{
The rows appended (or that would be), invisibly.
}
\description{
Writes names into the \code{datasetsInCompilation} tab so their timeseries appear
in the QC tab on the next run, each with \code{inThisCompilation = FALSE}. This is
the first half of the two-step: it makes a dataset \emph{visible} to the leads, who
then admit individual timeseries on the QC tab. It never adds anything to a
compilation.
}
\details{
Appends, and skips names already listed, so it is safe to re-run. Nothing is
rewritten: the tab holds thousands of rows of other people's curation.
}
