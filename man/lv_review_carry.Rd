% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_io.R
\name{lv_review_carry}
\alias{lv_review_carry}
\title{Carry decisions from an old review file into a newly generated one}
\usage{
lv_review_carry(from, to, proposals = TRUE)
}
\arguments{
\item{from}{Path to the review holding the decisions.}

\item{to}{Path to the newly generated review, or the tibble itself.}

\item{proposals}{Also carry the \verb{proposed_*} columns where the target has none.}
}
\value{
The merged tibble, with a \code{carried} attribute naming what moved and
what did not.
}
\description{
A review file is generated once and then owned by the person editing it, so
\code{\link[=lv_vocab_review]{lv_vocab_review()}} refuses to overwrite. That is the right default and it
makes one thing awkward: when the review format gains something worth having
-- example values, a better ordering -- the choice is between losing recorded
decisions and going without.
}
\details{
This copies the decision side across by \verb{(field, value)}, leaving everything
else to come from the new file. Only rows that are actually decided are
copied, and a row already decided in the target is never overwritten.

Values that no longer appear are reported rather than dropped silently: they
usually mean the new file was generated from a different batch, which is
worth noticing before deciding anything on top of it.

Proposals are carried too. They are inert -- nothing reads the \verb{proposed_*}
side but a person -- and the rationale behind a suggestion is the context
that made it reviewable in the first place.
}
