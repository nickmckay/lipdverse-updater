% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csm.R
\name{csm}
\alias{csm}
\title{Compilation-specific metadata in the merge}
\description{
A csm field is a judgement a compilation makes about a timeseries, stored
inside the \code{inCompilation} entry that already records membership:
}
\details{
\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{"compilationName": "hydroclimate2k",
 "compilationVersion": ["1_0_0"],
 "csm": \{"QCCertification": "NPM"\}\}
}\if{html}{\out{</div>}}

The registry names both halves: \code{csm_compilation} says whose it is and
\code{csm_field} says what it is called inside \code{csm}. The QC sheet and the store
keep using the field's \code{qc_name}, so a curator sees one column and the store
keeps one cell per timeseries, exactly as for a merged field.

Until this file existed, csm was declared everywhere and merged nowhere: the
registry named the fields and \code{\link[=lv_compilation_sheet]{lv_compilation_sheet()}} put the columns on a
new sheet, but \code{\link[=qc_frame]{qc_frame()}} read only roles \code{merged} and \code{key} and
\code{\link[=qc_merge]{qc_merge()}} dropped role \code{csm} outright. A curator's certification sat on the
sheet and reached no file. \code{\link[=lv_csm_frame]{lv_csm_frame()}} and \code{\link[=lv_apply_csm]{lv_apply_csm()}} are the two
missing ends; the merge itself needed only to stop discarding the role.
}
\section{Scoping}{

Every end is scoped to one compilation, so a cross-compilation csm conflict
cannot arise rather than being resolved. A QC sheet can carry a column
belonging to another compilation -- sheets get copied from one another, and
the registry holds a synonym for iso2k's certification column -- and without
the scope a curator editing hydroclimate2k's tab would write into iso2k's
\code{csm}, which no iso2k lead would ever see.

The exclusion happens at the sheet, in \code{\link[=lv_csm_scope]{lv_csm_scope()}}, before the merge:
a foreign cell that reaches the plan also reaches \code{\link[=qc_plan_state]{qc_plan_state()}}, and from
there the store and the sheet push, so refusing it at write time is too late.
\code{\link[=lv_apply_csm]{lv_apply_csm()}} refuses it as well, as the last check before a file changes.

Only the sheet is scoped, never the store. A cell the store holds and the
resolved state does not becomes a tombstone in \code{\link[=qc_diff_to_events]{qc_diff_to_events()}}, so
filtering the baseline would record deletions of another compilation's values
rather than ignoring them.
}

\section{What is not read here}{

Only \code{csm} is read. The pre-migration flat keys (\code{paleoData_QCCertification}
and the compilation-private spellings beside it) are deliberately not a
fallback: those values were replicated into every member compilation by the
migration, so reading them back would let one compilation's certification
arrive as another's. Where a compilation's csm needs seeding, it is seeded
from that compilation's own QC sheet.
}

