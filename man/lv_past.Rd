% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/past.R
\name{lv_past}
\alias{lv_past}
\alias{lv_past_path}
\title{The PaST thesaurus}
\usage{
lv_past(
  path = lv_past_path(),
  cache = fs::path(lv_path("state"), "cache", "past.rds")
)

lv_past_path()
}
\arguments{
\item{path}{The \code{past.json} file.}

\item{cache}{Cache file; \code{NULL} to skip caching.}
}
\value{
A tibble of \code{pastId}, \code{pastName}, \code{altLabels}, \code{definition}, \code{broader}.
}
\description{
NOAA's Paleoenvironmental Standard Terms thesaurus, as SKOS JSON-LD. The
vocabulary alignment sheets carry \code{paleoData_pastName} and \code{paleoData_pastId}
columns, so a term proposed for the vocabulary is more useful with a PaST
alignment attached than without one.
}
\details{
Parsed once into a flat tibble and cached, because \code{past.json} is 3.3 MB of
deeply nested JSON and re-parsing it per lookup is what made
\code{getStandardTables()} slow in lipdverseR.
}
