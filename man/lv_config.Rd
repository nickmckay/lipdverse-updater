% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{lv_config}
\alias{lv_config}
\title{Build the configuration for one compilation}
\usage{
lv_config(compilation, ...)
}
\arguments{
\item{compilation}{Compilation name.}

\item{...}{Overrides applied last.}
}
\value{
An \code{lv_config} object.
}
\description{
Layers, later winning: package defaults, the registry row, an optional
per-compilation YAML, then any \code{...} overrides.
}
\details{
Replaces \code{buildParams()}, which captured its own arguments with
\code{ls()} + \code{map(eval(parse(text = .x)))} into an untyped list that every stage
then splatted into its local frame with \code{assign()}. Nothing validated the
contents and nothing declared what a stage required.
}
