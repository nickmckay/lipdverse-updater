% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_compilation.R
\name{lv_create_compilation}
\alias{lv_create_compilation}
\title{Create a compilation from a list of TSids}
\usage{
lv_create_compilation(
  tsids,
  compilation,
  version = "1_0_0",
  dir = lv_path("database"),
  out,
  index = NULL,
  progress = TRUE
)
}
\arguments{
\item{tsids}{TSids to include.}

\item{compilation}{Compilation name.}

\item{version}{Compilation version to record, e.g. \code{"1_0_0"}.}

\item{dir}{Source database directory.}

\item{out}{Staging directory.}

\item{index}{An \code{lv_index}, to avoid re-reading the database.}

\item{progress}{Show progress.}
}
\value{
A list of \code{issues}, \code{datasets} touched, and \code{tsids} placed.
}
\description{
Adds an \code{inCompilation} entry to every named column. This is the common way
a compilation begins: someone has a list of timeseries and wants them
gathered under a name.
}
\details{
The write is \strong{additive}. An existing membership for the same compilation is
updated in place (its version list gains the new version); memberships for
other compilations are untouched. Nothing else in the file changes.

Never writes in place: output goes to a staging directory for \code{\link[=lv_promote]{lv_promote()}}.
}
