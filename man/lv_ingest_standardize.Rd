% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{lv_ingest_standardize}
\alias{lv_ingest_standardize}
\title{Standardize vocabulary in staged files}
\usage{
lv_ingest_standardize(
  dir,
  out,
  vocab = lv_vocab_overlay(store = store),
  store = qc_store(),
  remap = lv_vocab_remap(store),
  progress = TRUE
)
}
\arguments{
\item{dir}{Directory of files to standardize, usually a staging directory.}

\item{out}{Output directory. Never writes in place.}

\item{vocab}{From \code{\link[=lv_vocab]{lv_vocab()}}.}

\item{progress}{Show progress.}
}
\value{
A list of \code{changes} (a tibble), \code{issues}, and the \code{pin} used.
}
\description{
Replaces \code{standardizeLipdBatch()}, which reaches the values by round-tripping
the dataset through \code{as.lipdTsTibble()} / \code{as.lipd()}. That round trip pads
every column to the dataset's maximum interpretation count, inventing empty
entries with \code{scope = NA} -- the mechanism behind the empty interpretation
shells in the database, and a live one: it adds them to every file it touches,
including \code{year} columns. This walks the object and assigns six fields.
}
\details{
Reports rather than guesses: an unmatched value is left exactly as it is and
returned as an issue, for a curator to resolve into the vocabulary.
}
