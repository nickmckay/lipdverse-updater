% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab_app.R
\name{lv_vocab_review_app}
\alias{lv_vocab_review_app}
\title{Review vocabulary decisions in a browser}
\usage{
lv_vocab_review_app(path, vocab = lv_vocab(), launch = TRUE, port = NULL)
}
\arguments{
\item{path}{A review file from \code{\link[=lv_vocab_review]{lv_vocab_review()}}.}

\item{vocab}{From \code{\link[=lv_vocab]{lv_vocab()}}; used to validate a hand-entered \code{map_to}.}

\item{launch}{Open a browser.}

\item{port}{Port to serve on.}
}
\value{
With \code{launch = TRUE}, invisibly the path, after the browser session
ends. With \code{launch = FALSE}, the Shiny app object, so the decision logic can
be exercised with \code{\link[shiny:testServer]{shiny::testServer()}}.
}
\description{
A local Shiny app over a review file from \code{\link[=lv_vocab_review]{lv_vocab_review()}}. It exists
because the review is badly served by a spreadsheet: the context needed to
rule on a value is a paper, a PaST definition and a corner of the vocabulary,
none of which fit in a cell, and because the rows are far more repetitive than
they look. The hydroclimate2k batch is 87 rows but only nine distinct
decisions, 45 of them one seasonal-precipitation pattern applied over and
over.
}
\details{
So values are grouped by the decision they would take, and a group is ruled on
once. Rows an agent left undecided are grouped by field and shown with
whatever it did find.

Writes only the \code{decision} side of the file, the same columns you would fill
in by hand, and never applies anything. \code{\link[=lv_vocab_apply_review]{lv_vocab_apply_review()}} is still a
separate, deliberate step.
}
