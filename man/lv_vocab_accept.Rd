% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab_review.R
\name{lv_vocab_accept}
\alias{lv_vocab_accept}
\title{Accept proposed vocabulary decisions}
\usage{
lv_vocab_accept(
  path,
  which = NULL,
  min_confidence = c("high", "medium", "low"),
  dry_run = TRUE
)
}
\arguments{
\item{path}{A review file.}

\item{which}{Optional character vector of \code{value}s to accept. Default accepts
every proposed row permitted by \code{min_confidence}.}

\item{min_confidence}{Accept only proposals at or above this confidence.
One of \code{"low"}, \code{"medium"}, \code{"high"}. Rows with no stated confidence are
treated as \code{"low"}.}

\item{dry_run}{Report what would be accepted without editing the file.}
}
\value{
The updated review tibble, invisibly.
}
\description{
Copies the \verb{proposed_*} columns of a review file into the \code{decision} columns,
which is the only side \code{\link[=lv_vocab_apply_review]{lv_vocab_apply_review()}} reads. This is the step where
a suggestion becomes your call, and it is deliberately explicit: an agent can
fill in every proposal in the file and nothing happens until this runs.
}
\details{
Rows that already carry a \code{decision} are never overwritten.
}
