% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_bundle.R
\name{lv_export_bib}
\alias{lv_export_bib}
\title{Write a BibTeX bibliography for a compilation}
\usage{
lv_export_bib(
  publications,
  path,
  datasets = NULL,
  references = lv_references(),
  progress = TRUE
)
}
\arguments{
\item{publications}{The \code{publications} table.}

\item{path}{Output \code{.bib} path.}

\item{datasets}{Optional dataset ids to restrict to, so the bibliography
matches the bundle.}

\item{references}{The reference database, from \code{\link[=lv_references]{lv_references()}}. Fills in
what the LiPD records lack, which is a third of their titles.}

\item{progress}{Show progress.}
}
\value{
The path, invisibly.
}
\description{
Built from the publications the export already carries rather than fetched
from DOIs. The legacy \code{.bib} was resolved through crossref, which is why it
has publishers and months this one does not; against that, this one needs no
network, cannot drift from the metadata it describes, and says exactly what
LiPDverse holds. A consumer wanting the richer record has the DOI.
}
